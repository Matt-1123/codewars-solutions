// 10.21.20
// Return a name

// JS:
const greet = (name) => `Hello, ${name} how are you doing today?`;

// Java:

// Python:
def greet(name):
    return "Hello, " + name + " how are you doing today?"